const add = (firstNumber,secondNumber) =>{
   const added = firstNumber + secondNumber
        return added
}

const multiplyByTwo = (number) => {
    const multiplied = number * 2
        return multiplied
}
add(2,3)
const twoPlusThree = 5
console.log(multiplyByTwo(twoPlusThree))
